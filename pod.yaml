apiVersion: v1
kind: Pod
metadata:
  name: secretenv
spec:
  serviceAccount: service-account
  volumes:
  - name: secretvolume
    secret:
      secretName: mysqlcontainer
  containers:
  - name: secretcontainer
    image: gcr.io/perceptive-day-252709/springbootgcp:latest
    env:
      - name: SECRETS_USER
        valueFrom:
          secretKeyRef:
            name: mysqlcontainer
            key: username
      - name: SECRETS_PASSWORD
        valueFrom:
          secretKeyRef:
            name: mysqlcontainer
            key: password
  restartPolicy: Never
  