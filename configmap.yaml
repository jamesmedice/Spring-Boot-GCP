apiVersion: v1
kind: ConfigMap
metadata:
  name: invoice
data:
  application.yml: |+
    server:
      port: 9000
    datasource:
      driverClassName: com.mysql.cj.jdbc.Driver
      username: ${SECRETS_USER}
      password: ${SECRETS_PASSWORD}